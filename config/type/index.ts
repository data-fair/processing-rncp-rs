// Hand-written types for the plugin config.
// Can be regenerated from ./schema.ts with `npm run build-types` (outputs to ./.type).
export interface Config {
  dataFairUrl: string
  dataFairAPIKey: string
}
